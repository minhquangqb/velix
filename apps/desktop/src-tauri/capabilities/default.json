{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the trusted local shell UI and tray popup",
  "windows": ["main", "tray"],
  "webviews": ["ui", "tray"],
  "permissions": ["core:default"]
}
